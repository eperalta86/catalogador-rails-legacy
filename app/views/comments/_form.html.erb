<%= stylesheet_link_tag "bootstrap" %>
<%= form_for([@item, @item.comments.build]) do |f| %>
  <p>
    <%= f.label "Añadir un comentario" %><br />
    <%= f.text_area :mycomment, :size => "45x5" %>
    <%= f.submit "Guardar" ,:class => "btn btn-primary"%>
  </p>
<% end %>